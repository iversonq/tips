import 'package:flutter/material.dart';

class TripSummaryScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Trip Summary Page', style: TextStyle(fontSize: 24)),
    );
  }
}
